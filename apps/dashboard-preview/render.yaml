services:
  - type: web
    name: dashboards-library
    runtime: node
    rootDir: apps/dashboard-preview
    plan: starter
    branch: main
    autoDeploy: true
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false