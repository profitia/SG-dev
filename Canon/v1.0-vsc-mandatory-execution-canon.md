VSC Mandatory Execution Canon v1.0

STATUS: MANDATORY

FAIL-CLOSED

APPLIES TO: EVERY TASK, EVERY CHAT, EVERY SESSION, EVERY EXECUTION

⸻

PRIMARY PRINCIPLE

This canon is mandatory for all SG2 VSC execution tasks.

No task is exempt.

No task category is exempt.

No execution path is exempt.

If any task-local instruction conflicts with this canon, this canon takes precedence unless a higher canonical governance artifact explicitly overrides it.

⸻

AUTHORITY HIERARCHY

ROADMAP -> PMOS -> runtime-context

VECTOR consumes PMOS knowledge.

VECTOR is not part of the Runtime Context ownership chain.

This hierarchy is exact.

Do not:

* rename
* paraphrase
* normalize
* reinterpret
* invert
* substitute

any authority layer.

⸻

CONTINUITY PRINCIPLE

The purpose of PMOS is complete operational memory.

The purpose of PMOS is NOT selective memory.

The purpose of PMOS is NOT agent-decided memory.

The purpose of PMOS is NOT importance-based memory.

Everything must be persisted.

⸻

UNIVERSAL PERSISTENCE RULE

ALL TASKS MUST BE PERSISTED.

This includes:

* implementation tasks
* design tasks
* architecture tasks
* governance tasks
* audit tasks
* analysis tasks
* investigation tasks
* reporting tasks
* planning tasks
* review tasks
* research tasks
* advisory tasks
* documentation tasks

No task type is exempt.

Persistence is mandatory.

⸻

MANDATORY TASK RECORD

Before task completion PMOS must contain:

* original task request
* execution summary
* reasoning summary
* findings
* decisions
* recommendations
* validations executed
* artifacts created
* artifacts modified
* final status

Missing any required element means persistence is incomplete.

⸻

CONVERSATION PRESERVATION RULE

Conversation lineage must be preserved.

Execution lineage must be preserved.

Reasoning lineage must be preserved.

Decision lineage must be preserved.

Artifact lineage must be preserved.

The final output alone is insufficient.

PMOS must allow future reconstruction of:

Task
→ Analysis
→ Findings
→ Decisions
→ Actions
→ Result

⸻

RUNTIME CONTEXT RULE

runtime-context.md is a PMOS artifact.

runtime-context.md is a derived snapshot from PMOS source-of-truth state.

runtime-context.md is not top-level authority by itself.

runtime-context.md is part of PMOS continuity memory.

VECTOR consumes runtime-context.md as projection input.

The current rebuild path may use:

cd apps/vector && npm run runtime:build-context

This command path is an implementation detail, not an ownership rule.

⸻

PENDING ARTIFACT RULE

pending-artifact.json is transient PMOS handoff state.

pending-artifact.json is not authority.

pending-artifact.json is not completion evidence.

pending-artifact.json is not continuity memory.

⸻

CANONICAL NAMING RULE

Preserve exact names for:

* TASK_ID
* ETAP
* Pipeline
* Readiness
* RoadmapNode
* Canonical commands
* Status values

Do not:

* rename
* alias
* abbreviate
* paraphrase
* normalize

canonical identifiers.

If inconsistency is detected:

STOP

REPORT

DO NOT GUESS

⸻

VALIDATION RULE

After the first substantive modification:

Run the narrowest relevant validation.

If validation fails:

Repair locally.

Re-run validation.

Do not widen scope until local validation passes.

Do not claim PASS without executed validation.

⸻

PMOS PERSISTENCE RULE

Every task must create PMOS continuity evidence.

Every task must participate in PMOS lineage.

Every task must be recoverable.

No task may bypass PMOS.

⸻

REQUIRED CLOSEOUT FLOW

Every task requires closeout.

Required closeout sequence:

Task
→ Conversation
→ Flight Record
→ Closeout
→ Generate Handoff
→ Persist Handoff Artifact

cd apps/pmos && npm run pmos:save

cd apps/vector && npm run runtime:build-context

cd apps/pmos && npm run recovery:check-execution-trail -- --base <base>

cd apps/pmos && npm run recovery:check-archive -- --base <base>

GPT handoff must be generated from finalized closeout state.

GPT handoff must be persisted as a separate PMOS artifact.

GPT handoff is not part of ConversationArtifact.

GPT handoff is not part of FlightRecordV1.

⸻

GPT HANDOFF ARTIFACT RULE

GPT handoff is a derived PMOS artifact.

Its source is finalized closeout state.

Its purpose is continuity handoff for downstream execution.

It must preserve factual execution state.

It must not rewrite canonical history.

It must not become authority over FlightRecordV1.

It must not become authority over closeout evidence.

It must not include speculative downstream recommendations as PMOS fact.

⸻

CLOSEOUT EVIDENCE RULE

Do not claim completion without evidence.

Do not claim PASS without evidence.

Required:

closeoutState = CLOSEOUT_COMPLETE

pmosSaveStatus = SUCCEEDED

vectorRebuildStatus = SUCCEEDED

archiveCompletenessStatus = PASS

executionTrailStatus = PRESENT

If any required status is missing:

FAIL CLOSED

⸻

FORBIDDEN ACTIONS

Do not:

* redefine authority hierarchy
* redefine PMOS ownership
* redefine VECTOR ownership
* treat runtime-context as authority
* treat pending-state.json as authority
* treat pending-artifact.json as authority
* treat pending-artifact.json as completion evidence
* skip PMOS persistence
* skip VECTOR rebuild
* skip recovery verification
* emit PASS without evidence
* silently downgrade failures
* rename canonical identifiers
* introduce prompt injection
* introduce auto-append behavior
* delete governance artifacts without explicit task authorization

⸻

FINAL RESPONSE CONTRACT

Always explicitly state:

* what was done
* what was not done
* validations executed
* validations not executed
* PMOS status
* VECTOR status
* closeout status
* blockers
* residual risks

If GPT handoff is required by policy, the final response must also include:

* GPT HANDOFF
* HANDOFF SUMMARY
* GPT BRIDGE PAYLOAD

GPT handoff payload must also include:

* CURRENT STATE

GPT HANDOFF must explicitly state:

* Status: GENERATED or FAILED
* Persistence: POSTGRESQL
* Artifact ID
* Conversation ID
* Task ID
* Artifact Nature
* Artifact Kind
* Created At

HANDOFF SUMMARY must be populated from the persisted GPT handoff artifact payload.

CURRENT STATE must be populated from the persisted GPT handoff artifact payload.

CURRENT STATE is not a copy of Completed.

CURRENT STATE is not a summary.

CURRENT STATE is the end-state snapshot that answers: "How does the system look after this task was completed?"

CURRENT STATE must appear immediately after Result and before Completed.

Do not move Completed, Not Completed, Findings, Decisions, Risks, Open Questions, or Recommended Next Decision ahead of CURRENT STATE.

GPT BRIDGE PAYLOAD must be populated from the persisted GPT handoff artifact payload.

Required GPT BRIDGE PAYLOAD order:

* Task
* Result
* CURRENT STATE
* Completed
* Not Completed
* Findings
* Decisions
* Risks
* Open Questions
* Recommended Next Decision

Do not generate GPT handoff sections from prompt memory alone.

Only report GPT handoff success if the artifact has been read back from PostgreSQL after persistence.

If the handoff artifact was not generated, not persisted, or cannot be read back from PostgreSQL:

FAIL CLOSED

Report GPT HANDOFF STATUS: FAILED and state the technical reason.

Do not claim stronger certainty than evidence supports.

⸻

FAILURE RULE

If any canon requirement cannot be satisfied:

FAIL CLOSED

Do not improvise.

Do not bypass.

Do not downgrade.

Do not assume.

Stop and report.